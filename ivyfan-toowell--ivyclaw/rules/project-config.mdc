---
description: 本文件记录项目长期有效的工程约束。除非用户明确要求，否则必须遵守。
alwaysApply: true
---

# IvyClaw / DevMate 项目约定

本文件记录项目长期有效的工程约束。除非用户明确要求，否则必须遵守。

## 项目结构

- 技术栈：Python 3.13、FastAPI、Pydantic。
- 依赖和命令统一使用 `uv`。
- Agent 核心位于 `agent/`。
- 业务源码位于 `app/`。
- 测试代码位于 `tests/`。
- 中间件位于 `middleware/`。
- 项目 Skills 位于 `skills/`。

## 模块职责

- `app/main.py`：创建 FastAPI 应用并注册路由。
- `app/routes.py`：处理 HTTP 请求、响应和业务调用。
- `app/schemas.py`：统一存放请求与响应模型。
- `app/pricing.py`：统一存放定价和金额计算逻辑。

新增类型、函数或路由前，先搜索项目中是否已有可复用实现。不得重复定义相同职责的模型、路由或业务逻辑。

## 编码规范

- 所有货币金额使用 `Decimal` 计算，禁止使用 `float` 进行货币运算。
- 所有公开函数和公开方法必须提供类型注解。
- 所有公开函数和公开方法必须提供简洁、准确的 docstring。
- 类型注解必须与运行时实际传入的数据结构一致。
- Python 内部变量、函数和字段使用 `snake_case`。
- 对外 API 字段使用 `camelCase`，通过 Pydantic alias 映射。
- 结构化请求和响应使用明确的 Pydantic 模型，不使用裸 `dict`。
- 新代码使用 Pydantic v2 API，例如 `model_dump()`。
- 不使用 `except Exception` 将所有异常统一转换成客户端错误。
- 不随意改变已有 API 的路径、方法、字段名或响应结构。

## 测试要求

修改业务行为时，必须同步新增或更新对应的 pytest 测试。

测试应覆盖：

- 正常路径；
- 与任务相关的边界场景；
- 与任务相关的异常场景；
- 已修复问题的回归场景。

不得通过删除测试、降低断言标准或修改正确预期来掩盖业务错误。

## 验证要求

完成代码修改后，根据改动范围运行适用的验证命令：

```powershell
uv run python -m compileall -q app agent middleware scripts tools
uv run pytest -q
```

如果项目已经配置 Ruff 或类型检查器，也必须运行对应检查。

检查失败时，应定位原因、修复问题并重新运行。未通过必要验证时，不得声称任务已经完成。

## 修改原则

- 优先进行满足当前任务所需的最小修改。
- 修改公开函数或接口前，检查相关调用点。
- 优先复用已有模块、类型、函数和公共能力。
- 不进行与当前任务无关的重构。
- 不覆盖用户已有的无关修改。
- 除非用户明确要求，不执行提交、推送、发布、删除或创建 Pull Request。

---
> Source: [ivyfan-toowell/IvyClaw](https://github.com/ivyfan-toowell/IvyClaw) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:cursor_mdc:2026-09-13 -->
