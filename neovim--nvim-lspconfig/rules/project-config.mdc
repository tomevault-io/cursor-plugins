---
description: Everything in `lua/lspconfig/*` (except `util.lua`) is frozen legacy code;
alwaysApply: true
---

Everything in `lua/lspconfig/*` (except `util.lua`) is frozen legacy code;
ignore that subtree when looking for bugs or analyzing patterns in the
codebase.

---
> Source: [neovim/nvim-lspconfig](https://github.com/neovim/nvim-lspconfig) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:cursor_mdc:2026-07-21 -->
